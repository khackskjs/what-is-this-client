{
  "exclude": ["node_modules"],
  "compilerOptions": {
    // This must be specified if "paths" is set
    "baseUrl": ".",
    // Relative to "baseUrl"
    "paths": {
      "@services/*": ["./src/services/*"]
    }
  }
}
